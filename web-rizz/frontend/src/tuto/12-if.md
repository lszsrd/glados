---
category: Control Flow
title: IF Expressions
filename: if.rz
code: |
  fn foo(Int: x, Int: y) -> Int {
    if (y == 0) {
      ret 0;
    }
    ret x / y;
  }

  fn main() -> Int {
    ret foo(10, 2);
  }
---

# IF Expressions

Use `if` to execute code conditionally. The condition must be wrapped in parentheses.

In this example, we check if `y` is zero to avoid division by zero.
