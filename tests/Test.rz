struct bar2 {
    Int: i,
    Int: j
}

struct bar {
    bar2: i,
    Char: c
}

struct foo {
    bar: b,
    Int: x,
    Int: j
}

 
fn babar(Int: x) -> Int
{
    Int y = 5;
    bar2 f = {1,1};
 
    
    f->i = 1;
    for (Int q = feur(y); feur(y) < f->i; y++) {
        if (0) {
            ret 0;
        }
        if ((4 > 2) == 0) {
            ret 1;
        }
        if (True) {
            ret 12;
        }
    }
    y = feur(y);
    ret y;
}
 
fn baz()
{
    [Int] s = [1, 2, 56];
    bar2 ll = {1, 50};
    Int foo2do = 1;

    if (ll == 0) {
        ret ;
    }
    // ll->i = -5;
    // s[1] = 3;
    // ret babar(foo(100) + 4 - 5) * feur(10) + 4 - 5 / 1;
}
