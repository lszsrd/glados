fn foo(Int: x) -> Int
{
    Int y = 42;
 
    if (x == 0) {
        ret 0;
    }
    ret y / x;
}
 
fn bar(Int: x) -> Int
{
    Int y = 1;
 
    for (; y < x; y++) {
        if (foo(y) == 0) {
            ret 10;
        }
    }
    ret y;
}
 
fn baz()
{
    bar(foo(100));
}
