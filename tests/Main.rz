fn foo(Int: bar, Int: baz) -> Int
{
    if (baz == 0) {
        ret 0;
    }
    ret bar / baz;
}

fn main() -> Int
{
    Int a = 2;
    Int b = 3;
    Int c = 4;

    for (Int i = 0; i < 3; i++) {
        a += foo(a, c);
    }

    ret a;
}
