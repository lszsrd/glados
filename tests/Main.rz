/* fn feur(Int: a, Int: b) -> Int
{
    fn kk(Int: z)-> Int
    {
        Int oiiai = z * 12;
        ret oiiai;
    }

    ret kk(a);
} */

// divide two integers
fn      foo(Int: x, Int: y) -> Int
{
    [Int] i = [1, 2, 3];
    if (y == 0) {
        ret 0; // returns 0 to avoid div by 0
    }
    ret x / y;
}

/* comment block
            */
fn main() -> Int
{
    Int /*  */ x = 42;
    
    baz();
    ret foo(x, 2);
}
//endof file
