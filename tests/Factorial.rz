fn main() -> Int
{
    Char c = read(0);

    print(1, "factorial(5) = ");
    println(1, factorial(5));
    ret c;
}

fn factorial(Int: x) -> Int
{
    if (x == 1) {
        ret 1;
    }
    ret x * factorial(x - 1);
}
