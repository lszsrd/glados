fn main() -> Int
{
    dumpFile("LICENSE.md");
    ret 0;
}

fn dumpFile ([Char]: path)
{
    Int fd = open(path, "r");
    
    while (isEOF(fd) == False) {
        println(1, readln(fd));
    }
    close(fd);
}
