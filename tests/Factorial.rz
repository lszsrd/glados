struct Foo {
    Int: x,
    Int: y
}

/* struct Bar {
    Foo: coordinates,
    Int: baz
} */

fn main() -> Int
{
    Foo x = {1, 2};
    // Bar y = {{0, 0}, 0}; // VM MUST block this by disabling identifiers binding to structure fields

    println(1, x->x);
    x->x = 42;
    println(1, x->x);
    ret "OK";
}
