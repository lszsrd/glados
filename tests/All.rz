Int a = 0;

struct Foo {Bool:x, Char: z, Float: y}

fn callToAnother(Int: hugo)
{
    Char z = 'b';
    [Float] pi = [3.141592653589793238, 1.61803398874];
    ret;
}

fn main(Foo: ac, [[Char]]: av) -> Int
{
    Int hugo = 0;
    hugo = 1;
    hugo++;

    Foo test = {True, 'a', 1.61803398874};

    if (1 == hugo) {
        hugo--;
    } else {
        for (Int m = 10; m > hugo; m--) {
            foreach (v: av) {
                ret -1;
            }
        }
    }
    while (False) {
        if ((1.52 / 3) > 8) {
        }
        callToAnother(hugo);
        break;
    }
    printLaszlo(55 + 14, {True, 'a', 1.61803398874});
    ret 0;
}
