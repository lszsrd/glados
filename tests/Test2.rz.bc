FUNC foo 0      # function foo: always returns 42
PUSH 42
RET
ENDFUNC

FUNC bar 2      # function bar: checks if both parameters are equals and returns the computation
ENDFUNC

FUNC baz 0      # function baz: sums up foo and bar
CALL foo        # adds foo's return value to the stack
STORE bar_x
PUSH 42
STORE bar_y
CALL bar        # calling bar with foo's return value and 42
RET             # returns bar's return value
ENDFUNC
